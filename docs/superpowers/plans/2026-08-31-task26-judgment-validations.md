# 下一步计划：Task 2.6 三判决验证 + 二期闭合（含算力预算与失败归因预案）

> **闭合状态（2026-09-08）**：判决门经"修订版路径 1"收口完成，判定与能力声明见
> `docs/superpowers/specs/2026-09-08-task26-closure.md`（批复：
> `docs/superpowers/specs/2026-09-08-a0-pass-and-26-closure-decision.md`）。
> 力口径由"未验收"转"限定包络可用"；2.6.1 严格同密度跨基组读数登记开放项。

> 定位：二期收尾的判决性验证轮。**不写新功能代码**（唯一例外：归因发现的 bug 修复与 M3b 命运判决的执行）。
> 依据：phase2 计划 Task 2.6/2.7 + 评审链遗留项（c984b708e 未评审、037 回归与 sabotage 未复核）。
> FD 脚本已就位：`tests/constraint_fd_force/tools/run_constraint_fd.sh`、`run_constraint_torque_fd.sh`。

**Goal:** 用三个独立判决实验给二期能力定性：PW≡LCAO 逐位一致（口径不变量）、力 FD（物理精度）、力矩 FD（与 DeltaSpin 能力对等）；随后闭合二期。

**判据纪律：任何一项不过 → 归因排查，不降低判据（phase2 计划 §2.7）。**

---

## Task 2.6.0: 前置评审（半天，先于一切）

- [x] 评审 c984b708e（M3b 升格运行时审计）：(a) paraV 的 MPI 分布传递与 transferSerials2Parallels 正确性；(b) trace() 的位一致布局配对与拒绝路径；(c) 运行时审计是否接入 LCAO esolver 且默认开销可忽略；(d) 升格后单测仍绿。产出评审 spec（2026-09-07-task26-review-m3b-np4-fix.md；np=4 空目标段错误已修）。
- [x] 低风险未复核项清零：037_PW_FM 回归复跑（5/5）；三道 sabotage 重做一遍（评审者亲自，各恰中目标后恢复）。

## Task 2.6.1: PW≡LCAO 逐位一致（口径不变量，框架核心卖点）

- [ ] 同一 H₂O 几何、**同一收敛密度**（PW 导出 cube → 或直接同网格读数对拍）：两基组 Q_α 逐位一致 <1e-8；
- [ ] 同一约束靶点下两基组收敛 μ* 差 <1%（预期不完全相等——SCF 密度本身有基组差，μ 差属预期；逐位一致判据只针对同密度读数与算符，见 lcao-esolver-wiring spec §4 的口径声明）；
- [ ] M3b 运行时审计在此兑现：Tr[W^α·DM] vs ∫w_αρ 对拍（LCAO 侧，1e-10 级）；
- [ ] **数据留存**：这是增量 1（可发表点）的基准数据，输出机器可读块入库。

## Task 2.6.2: 力 FD（stationary4 协议，最重的一票）

**处方（R7，写死不得放宽）**：ecutwfc=100 + ecutrho≥400 + scf_thr=1e-8；δ=0.005 Bohr 中心差；冻结 t*（绝对模式跑位移腿，消除 μ·dQ_free/dR 污染）；E′=E_tot−μt*；判据 **|F_FD − F_ana| < 0.0128555 eV/Å**（5e-4 Ry/Bohr）。

- [x] 冒烟：`ONLY="0_2" bash run_constraint_fd.sh pw`（O 原子 z 方向单腿）先通，确认流程/单位/冻结逻辑（09-07 完成，见 09-07 主 spec）；
- [x] PW 全腿：H₂O 3 原子 × 3 轴 × ±δ = **18 腿**，每腿 = 一次约束 SCF（从 base 收敛密度 restart）——初跑 FAIL 经归因修复（SCC-μw，09-08 spec），**重跑 9/9 PASS**；
- [ ] LCAO 全腿：18 腿，同协议；
- [ ] 残余误差 ∝\|Q−t\| 检查：外环收敛容差收紧一档（1e-5）重跑一次单腿，残差应下降——验证误差受控于收敛容差（包络定理前提）。

**算力预算**：高网格（ecutwfc=100/ecutrho=400，15 Å 盒）下单次约束 SCF 估计 10–30 min（串行）/ 3–8 min（4 rank）；双基组 36 腿 + base + 容差档复跑 ≈ **40 次 SCF ≈ 4-rank 下 3–6 小时墙钟**（max_jobs=2 并行则减半）。可用 `ONLY` 分批。

**失败归因链（按序排查，DeltaP 先例）**：
1. escon 记账恒等式：E′(λ) 线性项 <2.5e-5 Ry/Ry（F-7b 先例）——账算不符则力必错；
2. 驻点条件：t* 冻结是否真生效（delta 模式泄漏 μ·dQ_free/dR 是已知 O(eV/Å) 级系统误差，脚本注释已警告）；
3. 网格精度：egg-box——低网格对照组（ecutwfc=50）若残差同量级则判据被噪声淹没，非力错；
4. 导数网格符号：回查 2.5.1 平移不变性自检是否仍绿（回归即此处）。

## Task 2.6.3: 力矩 FD（对标 DeltaSpin，μ=−λ 换算写死）

- [ ] `run_constraint_torque_fd.sh`：扰动靶点磁矩 δM（±0.01 μB 档），λ_解析=外环收敛 μ 值 vs ∂E/∂M 数值微分，判据 **<0.006 eV/μB**；
- [ ] **μ=−λ 换算**：本框架收敛 μ 与 DeltaSpin λ 符号相反（spin-channel spec §4 已预案）——对标时按 μ=−λ 换算，勿直接比；
- [ ] DeltaSpin 侧参照：同体系同靶点跑一次 DeltaSpin（既有功能）取其 λ 与力矩图（npjcm Fig.4b 口径）。

## Task 2.6.4: 自旋通道补验

- [ ] 自旋约束反假收敛（μ=0 自由跑不得收敛到非自然靶点——单测已覆盖，集成级补一遍）；
- [ ] LCAO 4-rank 逐位一致（冒烟已验收敛路径，此处验力/Q 数值一致性）。

## Task 2.7: 二期判决门 + 闭合

- [ ] 三判决全过 → 二期闭合：更新 capability 边界文档（力从"未验收"转"小窗可用"——窗口定义：\|μ\|≤0.5 Ry 且残差 <1e-4 的约束态）；用户手册 §5 边界第 1 条改写；
- [ ] 力 FD 超判据 → 按 2.6.2 归因链排查；**修复后重跑，不豁免**；
- [ ] PW≡LCAO 不一致 → 排 Gint 网格精度/密度口径；**不降低判据**（放水即否决架构核心卖点）。

## 二期闭合后的序列（本计划不展开，仅登记）

1. **V6 Au:Si 电荷态扫描**（原方案给电荷约束定的本职场景，用户价值最高的表征性实例）；
2. 混合约束 Task 2.4.1（JSON 逐约束 type，1–2 天，已有实测需求记录）；
3. 三期立项评估：应力（R6 推导先行）、偶极 V4（R5 协议细化）、Hirshfeld 权重、Broyden；
4. 多自旋约束收敛成本优化（47 外步实录）：κ 初值用先验（V3 已示 κ≈1.7 可作初值）或迟到的 Broyden 评估。

## 风险登记

| 风险 | 概率 | 应对 |
|---|---|---|
| 力 FD 超判据（历史最高发环节） | 中 | 归因链已备；最坏情况=发现真实力 bug，二期不闭合，力保持禁用 |
| FD 算力超预算 | 低 | ONLY 分批 + max_jobs 并行；先冒烟单腿再全腿 |
| 力矩 FD 口径混淆（μ/λ 符号） | 低 | 换算已在 spec 预案，判据卡死 |
| 高网格下外环变慢（scf_thr=1e-8 每腿 SCF 步数增加） | 中 | 预算已含；scaf_nmax 放宽到 300 |
