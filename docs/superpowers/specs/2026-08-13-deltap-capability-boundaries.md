# DeltaP 适用范围与能力边界（权威说明，2026-08-13）

> 本文档是 DeltaP 功能"能做什么、不能做什么、边界在哪"的唯一权威说明，
> 面向使用者与评审者。数值结论以本文档为准；主算法文档
> （`2026-08-05-deltap-algorithm-derivation-route-a-plus.md`）§8 已被本文取代。
> 版本基线：HEAD `217e8888f`（含 D_I/S_k/H_HK 三轮 MPI 修复、Route A+ 记账、
> A1/A2/B 力项、F-1/F-2/F-2b 场模式判决）。

---

## 0. 一句话定位

**DeltaP 当前是"小窗口精密约束 + 场模式能量/响应"工具：**
约束模式在自然极化附近的小窗口内（S C F 单点与 Γ-路径力）已验证可用；
场模式（均匀 λ 近似长程电场）的能量与极化响应已验证可用；
**大极化改变靶点、任意靶点 relax/MD（γ-hold 路径）、场模式力，当前均不可用；Γ-path relax 在自然靶点附近可用（4.3 已验）。**

---

## 1. 能力矩阵（按使用场景）

### 1.1 约束模式（deltap_corr=1，给定 γ 靶点）

| 场景 | 状态 | 边界/条件 |
|------|------|-----------|
| LCAO 单点 SCF（串行） | ✅ 可用 | 全部靶点类型（per-atom/total/约束矩阵/掩码/gdir 1-3）验证过 |
| LCAO 多 rank | ✅ 可用（D_I/S_k/HK 三轮修复 + F_HK 力 MPI 化后） | 非方本地块已解锁（656a2a59d）；E' 差 ≤2.5e-5 eV（FP 噪声经 λ 累积） |
| PW 单点/多 rank | ✅ 可用 | KPAR=1 硬限制；**PW 已切 Route A+ operator 记账（L1.1，2026-08-17：escon=−λΓ^PW，Γ^PW=⟨P̂^onsite⟩，λ 驱动保持 γ 残差）**；E'(λ) 平直性 @真 ecut=80 线性项 0.00014 Ry/Ry（F-7b，inner_thr=1e-6 口径）；无 target 文件/约束矩阵接线；total 为假模式 |
| 约束模式力（Γ-路径，小 λ） | ✅ 可用 | T3 判决：残差 0.0138 eV/Å（判据 0.0129 的 1.07×）；**Stage 4.2（2026-08-17）：hf/co/h2o_asym 三体系驻点 FD 全矩阵 21/21 腿 PASS，最大残差 0.00658 eV/Å（判据 1.95× 裕度），⟨η⟩ 关联阴性**；工作窗见 §2 |
| 约束模式力（γ-hold 路径） | ❌ 不可用 | T3' 实测 0.502 eV/Å（λ² 泄漏）；任何"严格钉 γ 的 relax"都走这条路径 |
| 内循环 λ 优化 | ⚠️ 受限 | 冻密度响应符号反转（−1.28 vs 自洽 +0.03 rad/Ry），冻密度 BFGS 对 γ 结构性失效；同步/SCF 级驱动可用 |
| relax/MD | ⚠️ 受限可用（Γ-path，自然靶点附近） | **Stage 4.3（2026-08-17）：Γ-path relax（t_Γ\*=自然 Γ 冻结）端到端 PASS**——h2o1 4 离子步收敛、轨迹与纯 DFT 逐点一致（能量差 ≤1.4e-4 eV、力差 ≤1.2e-3 eV/Å）、λ 全程 1e-5–1e-4 Ry；任意大靶点/γ-hold 仍不可用（弱耦合 λ 预算，T3' 0.502）；见 §4-F1 |

### 1.2 场模式（均匀 λ ≈ 长程电场）

| 场景 | 状态 | 边界/条件 |
|------|------|-----------|
| 能量/极化响应 | ✅ 可用（窗口内） | E₀(μ) 与锯齿场对拍 <1e-3 eV @ \|λ\|≤0.007 Ry（F-2）；E_eff=πλ/(2a)，实测校准 ×1.6 |
| 场模式力 | ❌ 挂起 | F-2b：力/极化 Maxwell 失配 17.7×（H_HK 能量含非极化几何通道）；第一性解法 = EFC（未立项）；半经验 Z\* 补丁可选（需标注非从头算） |
| 场模式应力 | ✅ 已实现（串行，内部自洽） | F-8（2026-08-17）：H_HK 应力 V-H7/Maxwell 闭合（残差 6.8–7.0% = C 响应，与 T3 力侧 7% 同源）；压电 efield 必测项：同体系杠杆 应力侧 1.78× vs 力侧 52.5× ⇒ 17.7× 类失配是力路径特有；MPI/ow 应力守卫拒绝；**使用含义（评审 2026-08-17）**：场模式应力的定量压电解释带 ~2× 场物理杠杆（对比力侧 52×）；约束模式的应力（内部自洽用途，如定 λ 变胞）不受此限 |

### 1.3 实验性/不可用

| 场景 | 状态 |
|------|------|
| ow 模式（Ô_w 精确权重算符） | ⚠️ 实验性：SCF/记账可用，**力未实现（F_ow 缺失）**；多 string 网格算符不完整（仅 string-0 覆盖）；应力 WARNING_QUIT |
| KPAR>1 | ❌ 守卫拒绝（k-string 跨全 k 列表） |
| 金属/近简并体系 | ❌ 未验证（分支/本征矢不稳定，理论上需 det 形式 fallback） |

---

## 2. 定量边界（工作窗）

### 2.1 约束模式

| 边界 | 数值 | 出处 |
|------|------|------|
| Γ-路径力残差 | 0.0138 eV/Å @ λ*≈1e-3 Ry；∝ λ² 增长 | T3 + 闭合率 1.07 |
| γ 可达域（h2o1，λ 预算 0.1 Ry） | O：Δγ∈[−0.004, +0.013] rad；**H：双侧死（±0.001）** | T-5' 窗口测绘 |
| γ-hold 力残差 | 0.502 eV/Å @ λ*≈0.02 Ry | T3'（闭合 96.7%） |
| 传递效率 dγ/dΓ | ≈0.07（自洽）；dγ/dλ≈0.028–0.05 rad/Ry（V1 全原子和口径 0.053）；刚度测量必须 γ-drive（proxy-drive κ=1 的 Γ-约束是 λ-work 伪曲率） | Q1/D1/D2 + 4.4 V1/V3 |
| γ 物理刚度 κ（h2o1，LCAO 基组） | ≈60 Ry/rad²（α_LCAO=3.02 Bohr³，紧缩基低估实验值 3.3×）；BN ≈0（V3 新记账复核：E' span 1.7 μRy、κ 噪声级，差 6 个量级） | D1/D2 + 4.4 V3 |

**窗口的用法**：小窗口 = 靶点靠近自然极化（界面反场补偿类应用正好落在这里）。
大窗口靶点（|Δγ| 大）当前不可达——主因是体系物理刚度 + 代理传递效率，
**不是 bug**；Ô_w 已实测不能扩大窗口（T-18）。

### 2.2 场模式

| 边界 | 数值 | 出处 |
|------|------|------|
| 能量窗口 | \|λ\|≤0.007 Ry：E₀(μ) 对拍偏差 <1e-3 eV；λ=0.01 时 1.55 meV | F-2 |
| E_eff 换算 | E_eff = πλ/(2a) a.u.（λ Ry，a 该方向周期 Bohr）；**V1（2026-08-17）符号钉死（两通道同号）、因子实测 1.60×（Σγ_raw 全原子和口径，与 D2 互证，μ₀=1.94 D 实验 5%）**；打印带响应校准注释，用户对拍 efield 时按注释换算 | D2 + F-5 + 4.4 V1 |
| 力偏差 | matched-μ 下 2.6%（λ=0.001）→ 21–26%（λ=0.01） | F-2/F-2b |
| dip_cor 协议 | efield 基线必须 amp=0；力对比必须 no-dip_cor 系列（镜像自能 +3.70 meV 常数） | F-2 协议教训 |

### 2.3 数值/验证设置边界

| 项 | 要求 |
|----|------|
| FD/验收设置 | LCAO：ecutwfc=100 + 显式 ecutrho≥400 + scf_thr=1e-8；PW：ecutwfc 80–100。低 ecut 只可用于隔离对照 |
| 基组 | 紧缩基（DZP 级）极化率低估 ~3×——极化率类结论须按基组定标，勿与实验 α 直接比 |
| MPI | LCAO 4-rank 方阵验证过（E' 差 ≤2.5e-5 eV，FP 噪声经 λ 累积，长轨迹可漂移）；非方网格被守卫拦 |
| 历史数据 | 2026-08-03 前的 λ 值受 B-6 影响需 ×1/L 重标定；之前的 E' 含 λ(Γ−γ) 污染 |

---

## 3. 已知限制完整表（LIMITATION）

| # | 限制 | 影响 | 出处/状态 |
|---|------|------|-----------|
| L1 | KPAR=1 硬限制 | k 点不能分组并行 | 守卫拒绝；Route W 预研是唯一出路 |
| L2 | ~~hk 路径方阵网格（nrow==ncol）~~ **已解除**（656a2a59d，pzgemm 分布式本地块） | 非方 rank 4-rank/2-rank 验证逐位一致 | 已修 |
| L3 | ~~F_HK 力串行-only~~ **已解除**（656a2a59d，行组 gather 全带对 Allreduce） | 场模式力 MPI 路径已通（h2o_asym 4-rank 与 2-rank 逐位一致） | 已修 |
| L4 | ~~H_HK 应力未实现~~ **已解除**（F-8，2026-08-17：串行 H_HK 应力，V-H7 内部 FD 残差 6.8% = C 响应；efield 压电对照完成，应力侧 1.78×，力侧 52.5×）；PW 应力由 onsite 路径覆盖（f48e59723） | 变胞/应力计算：hk 模式串行可用；hk+MPI、ow+应力守卫拒绝 | 已修（MPI 化未立项） |
| L5 | ~~PW 旧 gamma 记账~~ **已统一**（L1.1：escon=−λΓ^PW，驱动保持 γ）；**escon 恒等式精确（施加/记账同一 becp 路径，F-7b 证伪 1.784× 结构性失配假说）**；无 target 文件/约束矩阵接线；total 假模式 | PW 功能子集 | L1.1 ✅ + F-7b ✅ 2026-08-17；target 文件/约束矩阵接线未立项 |
| L6 | ow 模式：F_ow 未实现 + string-0 覆盖 + 应力拒绝 | ow 仅可用于 SCF 机制冒烟 | T-12/T-13 挂起（约束模式非当前目标） |
| L7 | 冻密度内循环对 γ 失效（响应符号反转） | 内循环 BFGS 仅 proxy/Γ 驱动可用 | T-7'' 实测钉死 |
| L8 | H 原子 Γ 代理方向性失控 | H 的大靶点单侧不可达 | T-2/T-5' 实测；Ô_w 不修 |
| ~~L9~~ **已修复（2026-08-17，根因级）** | ~~smoothness 单测 4/8 FAIL（此前归因 B-6 参考过期，实际根因：合成数据独立随机相位使 Wilson 乘积 \|prod\|≈0 + T5/T8 测试语义错误）~~ —— D_I 改正交归一 2×2 块、T5 改周期逐带规范、T8 改结构性扰动，8/8 PASS | 测试卫生 | ✅ 2026-08-17（见 `2026-08-17-deltap-anchor3-rebuild.md` §3.3） |
| L10 | 分支纪律：target-aware γ 报告是靶点跟随的 | 任何判据/FD 必须用连续性锚定读数 | 流程纪律（两次事故教训） |
| ~~L11~~ **已代码级修复（2026-08-17，历史注记）** | ~~escon/Γ 每 SCF 周期只在首个 drho<deltap_inner_thr 迭代测量一次~~ —— 已改为每 SCF iter_finish 在当前 ψ 上刷新（`refresh_pw_escon`，3 源文件 + 1 头文件）；F-7b 归因：非算符失配，是测量态伪影（默认 inner_thr=1e-3 下 Γ 偏差 0.1–0.3%）；修复后同档 E'(λ) 中心差 −0.0214 → −2.5e-5 Ry/Ry，inner_thr≤1e-6 规避不再需要 | F-7b（2026-08-17）归因 + Q1 修复（详见 `2026-08-17-deltap-f7b-escon-refresh.md`） |

| L12 | 解析力/应力的冻结-C 近似隙 ~7% | 解析侧（冻结 C）与 SCF 弛豫 FD 侧差 ~7%（C 响应 (∂E_HK/∂C)(dC/dε)，E_HK 非变分） | 同源证据：T3 闭合率 1.07、F-8 V-H7 6.8% / Maxwell 7.0%；改进路径 = 线性响应 C 导数（未立项，需求驱动） |
| L13 | ⟨η⟩ 使用建议（2026-08-17 按 4.2 阴性结果修订） | ⟨η⟩ 保留为 SMO 完备性结构指标；其 1% 经验线**不预测力残差**（CO 1.67% 最大残差 0.00541 vs ⟨η⟩=0 体系 0.00542/0.00658 eV/Å，同量级），**不作为力精度预警依据** | Stage 4.2 关联检查阴性（首次兑现检验）；仍可用于基组完备性诊断 |
---

## 4. 失效模式诊断表（用户遇到 X 时）

| 症状 | 原因 | 对策 |
|------|------|------|
| F1: relax 轨迹能量不降/力异常大 | 力路径超出窗口（γ-hold 或大 λ）；或 2026-08-17 前"无 target"跑被隐式 Γ→0 约束污染（已修复） | 用 Γ-path（deltap_proxy_target_file=自然 Γ + secant off）；无 target 现为真正自由跑；单步能量非单调若纯 DFT 同样出现 = 优化器过冲，非力场错误 |
| F2: 多 rank 报 "nrow != ncol" WARNING_QUIT | ~~方阵网格守卫（L2）~~ L2 已解除（656a2a59d），不应再出现 | 若复现 = 回归，报 bug |
| F3: "KPAR>1" WARNING_QUIT | L1 | kpar=1 |
| F4: γ 收敛到靶点但 λ=0 | target-aware 假收敛（L10） | 检查连续性锚是否激活（branch.dat 参考） |
| F5: ow 模式应力为 0 | L6 守卫（应力 WARNING_QUIT 前的旧版本静默缺失） | ow 模式不算应力 |
| F6: 与旧版本数值对不上 | B-6 τ 单位（2026-08-03 前 λ ×1/L 重标定）+ S_k 键修复（2026-08-04 前逐原子 γ 重分配） | 用当前版本重跑基线 |
| F7: 内循环振荡不收敛 | L7（冻密度响应反号） | 用同步/SCF 级驱动（deltap_inner_nmax=0） |
| F8: efield 对拍差常数 | dip_cor 镜像自能 | 基线取 amp=0（F-2 协议） |

---

## 5. 物理适用域总结

**适合**：
- 绝缘体/分子的极化约束与场响应（已验证：H2O 偶极 0.9%、BN 零刚度发现）；
- 界面/表面反向电场补偿（靶点≈自然极化，正在小窗口内）；
- 极化势能面采样（单点链，无 force 需求时窗口最宽）。

**不适合**：
- 大极化改变（铁电翻转全程、强场响应）——物理刚度 + 传递效率双重封顶；
- 任意靶点约束 relax/MD（力路径未通；Γ-path 自然靶点附近例外，4.3 已验）；
- 金属/近简并体系（分支理论未覆盖）。

**生态位备注**：分子/slab 的均匀场需求可先用 ABACUS 锯齿场（efield）满足；
DeltaP 场模式的独特价值在**真正 bulk 周期体系**（锯齿无处可放）——
但该定位的力侧交付以 EFC 为前提（未立项，见
`2026-08-07-deltap-route-a-plus-plus-review-and-plan.md`）。

---

## 本轮记录

- 文档合并轮，无代码改动。本文档取代主算法文档 §8 的 LIMITATION 清单；
  后续每轮功能/边界变化必须同步本文档（与 AGENTS.md 纪律同级）。
