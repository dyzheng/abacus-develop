# 2026-08-31 判决性验证 V1 / V3（T9/T11 完成，V2 工具阻塞）

## 1. Test plan（计划 Task 9-12 验收门）
- **T9 V1 sum rule**：H₂O 无约束 SCF，审计行 Σ_I N_I ≡ 8（<1e-8）。
- **T10 V2 口径基准**：同一收敛密度导出 cube，Multiwfn 算 Becke 电荷对拍
  （判据 1e-4 e）。外部工具依赖。
- **T11 V3 可达性 + 反假收敛**：delta 模式 q=±0.05/0.1/0.2/0.3 e 扫描全部
  达标无封顶；μ=0 自由跑不得收敛到非自然靶点；不可达靶点触发熔断。
- **T12 止损判决**：V3 可达域是否过窄（κ 封顶）。

## 2. Test setup
- 平台：abacus_basic_para 串行，H₂O 15 Å 盒，O.upf + H_ONCV_PBE-1.0.upf，
  ecutwfc=20 / ecutrho=80（81³ 网格），scf_thr=1e-7，scf_nmax=200。
- 约束：O 片段单约束，delta 模式，constraint_thr=1e-4，
  constraint_mu_max=5.0 Ry（V3 扫描）/ 0.5 Ry（熔断用例）。

## 3. Results
- **V1（T9）PASS**：`{"targets": [0,0,0], "atoms": [[0],[1],[2]]}` →
  参考 SCF 14 步收敛，审计行 `total_charge=8 nelec=8`（恒等，因密度
  归一化 + 单位分解 Σw_I≡1）；逐原子 Becke 电荷 O=6.25547、
  H=0.872266/0.872266；maxdev=2.2e-16；delta=0 立即 CONVERGED（无假收敛）。
- **V3（T11）PASS（7/7 可达）**：
  | delta/e | μ*/Ry | 外步数 | 终残差/e |
  |---|---|---|---|
  | +0.05 | −0.0870 | 4 | 2.3e-5 |
  | +0.1  | −0.1765 | 6 | 3.1e-5 |
  | +0.2  | −0.3637 | 9 | −7.1e-5 |
  | +0.3  | −0.5637 | 13 | −6.1e-5 |
  | −0.05 | +0.0848 | 4 | 3.7e-5 |
  | −0.1  | +0.1675 | 7 | −8.8e-6 |
  | −0.2  | +0.3268 | 8 | −1.5e-5 |
  | −0.3  | +0.4792 | 12 | 6.5e-5 |
  μ* ≈ −1.7·delta（响应平滑单调），|μ*|max=0.56 Ry ≪ 5.0 Ry 上限，
  全部无封顶。
- **熔断 PASS**：delta=+5.0 e（不可达）→ μ 顶到 −0.5 Ry 上限，Q 平台
  ~6.52 e（仅 +0.27 e），3 步平台窗无改进 → `UNREACHABLE (fused at mu cap
  0.5 Ry)`，附 Q(μ) 端点报告（res=−4.73）。
- **反假收敛 PASS**：全部 V3 用例仅在 Q 真达 target（|res|<1e-4）时报告
  CONVERGED；中间步骤从不提前收敛（M4 收敛检查先于更新）。
- **T12 判决**：V3 物理可达域宽（±0.3 e 无封顶，κ≈1.7 远离 0.3 下限），
  无止损触发，开二期条件满足。
- **V2（T10）阻塞**：本环境无 Multiwfn。协议已备：ABACUS `out_chg` 导出
  收敛密度 cube → Multiwfn "Becke 电荷" 对拍逐原子值。内部交叉验证由
  M2 单测覆盖（高斯基准 1e-8、单点 δ 钉 1e-12）。

## 4. Analysis
- V1 的 Σ_I N_I ≡ nelec 是"密度归一化 + 单位分解"双恒等式的直接结果：
  只要 Σ_α w_α ≡ 1 且密度已归一化到 nelec，sum rule 机器精度成立；
  该恒等式在每轮审计行中实时校验（maxdev + total_charge vs nelec）。
- V3 的 μ*∝delta 线性度说明 O 片段响应在 ±0.3 e 窗口内无强非线性；
  若二期需更大靶点，先验 κ≈1.7 可作为 M4 的 κ 初始值（当前从 κ_min 起）。
- 熔断行为正确：不可达通道 μ 顶限后 Q 平台（改进<1%/3 步）判定
  UNREACHABLE，与 M4 单测的死通道语义一致；报 Q(μ) 端点供物理分析。

## 5. Next steps
- V2：在有 Multiwfn 的机器上按上述协议对拍（判据 1e-4 e），补入本档。
- 二期：M3b（LCAO Gint 通道）、M6 力/力矩（stationary4 FD）、自旋通道
  （±μ 差分耦合，M3a 已预留）、PW≡LCAO 逐位一致。
