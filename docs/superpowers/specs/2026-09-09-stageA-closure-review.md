# Task A7 + 阶段 A 收口严格评审

> 评审对象：commit d1a246fc6（A7 文档收尾）+ 阶段 A 整体（A0–A7）。
> 本轮代码改动：仅清理 `tests/01_PW/213_PW_constraint_h2o_mixed/` 的未跟踪运行残留（OUT.autotest/log.txt，未入库文件，删除不影响任何跟踪内容）。

## 裁定：✅ 阶段 A 正式闭合（8/8）

## A7 核实

| 项 | 结果 |
|---|---|
| 提交与推送 | d1a246fc6 在 HEAD；zdy/feat/deltap = d1a246fc6c（推送属实） |
| 手册 §2/§3/§4/§5/§6 | diff 抽查：v1/v2 边界、审计 kind 标签、total_charge 混合语义修正、力限定包络、偶极守卫、213 用例——与代码实测行为逐条一致（评审此前各轮亲验的行为基准） |
| 开发者指南 | 数据模型/逐约束数据流/风险表刷新（channel run 级全局项移除）与 A1–A5 交付一致 |
| 阶段 A 总结文档 | 2026-09-09-stageA-progress-summary.md 结构与数据（11/11、sabotage 链、μ 耦合表、力包络）与评审链记录一致 |
| 计划勾选 | A7 三步 [x] + 状态注记如实 |

## 阶段 A 整体评估（对照批准的计划与评审修订）

1. **范围纪律满分**：偶极/Broyden/松紧 SCF 零抢跑；每个模块单一实现+薄适配，无第二路径残留（A2/A3/A5 逐轮核实）。
2. **验证链完整**：G1–G6 六门逐一把守，无一次带病前进；每守卫配 sabotage（累计 10+ 次恰中记录）；三旧用例逐位复现（v1 兼容链零漂移）。
3. **评审修订全部落实**：P1（47 步案例与块结构矛盾）→ 阶段 B 立项改为测量驱动，A6 μ 耦合实测已提供输入；P2（2.6 前置）→ 严格遵守；P3（跨类型耦合未验证）→ A6 实测结案；P4（Q 噪声预算）→ 随松紧 SCF 一并暂缓；P5（schema 迁移链）→ v1 自动转换+deprecation+三用例零修改。
4. **诚实文化保持**：偏差登记（A1 两处）、中间态登记（A4 两趟掩码）、恒等式弃用（A2 盆地漏磁）全部如实。

## 开放项（全部在案，阶段 A 不欠账）

① 混合力 stationary4 FD 验收轮（A 后第一轮重型验证）；② PW≡LCAO 只读观测口；③ torque 脚本 E' 修复；④ DeltaSpin 量级对等测量；⑤ 阶段 B 立项评审（输入=A6 μ 耦合表 + 47 步/15 步收敛数据）。

## 建议的下一步顺序

1. **混合力 FD 验收轮**（stationary4 处方，双基组）——阶段 A 功能的最后一块未验收能力；
2. 阶段 B 立项评审（Broyden/松紧 SCF，以实测数据决策）；
3. 视需求：偶极（阶段 C，R5 协议先行）或 V6 Au:Si 本职场景表征。

---

## 本轮记录

- 评审轮；仅清理未跟踪运行残留（卫生项闭环）。A6/A7 评审文档随本批归档。
