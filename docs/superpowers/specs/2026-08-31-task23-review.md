# 二期 Task 2.3 实证 Review（LCAO 接线 + 技术债下移）

> 评审对象：commit 308e509e4 完成报告；顺带补审未评审过的 2.1（f6fcc443d）与 2.2（17781e5da）。
> 方式：实跑复现 + 3 commit 代码评审，不采信报告文本。本轮无代码改动。

## 裁定：✅ 通过，可继续 Task 2.4。2 个待办（均小）+ 1 个架构观察项。

## 实证核实（报告声称 vs 实测）

| 声称 | 实测 | 结论 |
|---|---|---|
| LCAO 冒烟 6±2 步 CONVERGED、μ*=−0.2193、Q_ref=6.40796、maxdev=2.2e-16 | 复跑逐项吻合；FINAL_ETOT vs result.ref 偏差 2.4e-11 eV | ✅ |
| MPI 4-rank 与串行一致 ~1e-11 | 实测 3.4e-11 eV（对 ref 9.8e-12）——同量级，措辞建议"1e-11 量级" | ✅（措辞轻微） |
| PW 回归零偏差（<1e-7） | 实测 4.1e-9 eV | ✅ |
| constraint ctest 10/10 | 实测 10/10 PASS | ✅ |
| MODULE_LCAO 2 FAIL+2 Not Run 为既有环境问题 | 抽查证实：parallel_*.sh 未拷入构建树（CWD 类），commit 与相关目录零交集 | ✅ |
| TDD 失败态（旧二进制 0 条 [constraint] 行） | 无旧二进制可独立复核；旁证成立（当前二进制 7 条 [constraint] 行） | ⚠️ 无法复核但有旁证 |

## 代码评审要点

- 四钩子确为薄钩子（各 4–12 行），与 PW 通道逐行同构、仿 spinconstrain 生命周期位置；分支注释合规（含机制/时序说明）；注入时序正确（HSolver 前最后一次写 v_eff，无中间写者）。
- P2 契约完好：PW 原检查保留，LCAO 新增 `inject_potential_lcao` 同样检查返回值+WARNING_QUIT。
- PW −48 行配置块下移行为等价（PW 回归 4e-9 eV 佐证）；ConfigureFromInputsShared 五分支真断言。
- **补审 2.1/2.2（此前漏审）**：V2a 三档网格收敛真实交付（相邻档差 max 8.2e-5<1e-4，严格单调失败如实登记为口径偏差——诚实）；V2b 独立 Becke 参考真实（不调生产实现，<1e-8）；P1 勾选修正如实标注"替代完成"；Task 2.2 的 ΣW≡S 审计测试真实交付（2.0e-15/3.4e-15）。

## 待办与观察项

1. ⚠️ **计划勾选遗漏**：phase2 计划 Task 2.1 Step 6 仍为 `[ ]`（工作已实际完成）——补勾。
2. ⚠️ **M3b 架构观察项**：W^α HContainer 路径生产零调用（生产走 v_eff 网格注入+cal_gint_vl，该设计决策本身正确且更省——零新积分代码进生产路径）。当前 M3b 是"ctest 持续执行的审计仪器"而非死代码，但头注未披露生产旁路，易误导。建议：头注补一句声明；**Task 2.6 时升格为运行时审计**（Tr[W^α·DM] vs ∫w_αρ 对拍）或二期收尾评审时正式评估删除，不留悬置资产。
3. 轻微：MPI 一致性措辞；configure_from_inputs 错误消息文案微调（无行为影响）；build/commit.h 增量构建未刷新（非本次问题）。

## 对"网格注入"设计决策的评审意见

**认可**。v_eff 网格注入 + 生产 cal_gint_vl 的路径比手工 HContainer 加 H 更优：生产路径零新积分代码（更彻底地满足"零新组件"约束）、k 点自动同构、观测量==注入算符由单一 WeightGrid 实例构造保证（实证成立）。代价是失去显式 W^α 矩阵的矩阵元级审计能力——已由 Task 2.2 测试离线提供证据，建议在 Task 2.6 转为运行时审计补回生产侧自检。

## 结论

报告诚实、数据链三方互洽（spec/代码/实跑）、设计决策正确。**批准继续 Task 2.4（自旋通道）**，随 2.4 一并处理两个待办（补勾 + M3b 头注声明）。

---

## 本轮记录

- 评审轮，无代码改动；补审了此前漏审的 Task 2.1/2.2（均通过）。
