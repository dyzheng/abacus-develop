# 双迭代提交 + C-29 定位与修复 严格评审

> 评审对象：commits afb97690d / a95014bd9 + C-29 根因分析与工作树修复（rhog_io.cpp 守卫 + 回归测试）。
> 本轮无新代码改动（修复为被评审对象）。

## 裁定：✅ 全部通过。C-29 根因分析质量高，修复最小且语义正确。批准以 commit 3 落地，随后启动优先级 2（inner_thr 三档扫描）。

## 一、提交核实

- afb97690d（feat：双调度+settle+mix_reset，9 文件）与 a95014bd9（test：Q1–Q3 证据+决策表+文档）均已入库；证据目录裁剪合规（README/results/*.audit/summary.txt，无 SCF dump）；OMP=1 可复现性纪律已入开发者指南 §3.5。

## 二、C-29 根因核实与"清理路径"更正的接受

- **机制代码级属实**：`rhog_io.cpp:166` 区域，`fftixyz2ig[fftixyz]` 对"FFT 盒内但基组外"的平面波取 −1，越界写 `rhog[is][-1]`（缓冲区前 16 字节，malloc 头）。
- **我的"决算后清理路径"框架需要更正（接受）**：破坏点在 run **开头**（读更大基组写出的重启电荷文件），检测点在退出（Charge::destroy 的 free 触发已损坏的堆头）——这解释了为什么崩溃总在收尾、为什么父提交二进制逐位复现、为什么能量数据在崩溃前完整（只有堆元数据受伤）。更正后的事实链完全自洽。
- **触发链与历史吻合**：扫描战役复用高 ecut（160/640）电荷文件做低 ecut（60/240）run 的重启——C-29 历次出现（Mg 远侧、MgO δ=+0.5 双模式）全部落在该模式下。
- **修复评价**：`if (ig < 0) continue;`（含分支注释）——跳过当前基组不可表示的分量是唯一正确语义；金丝雀回归测试（不依赖 ASAN，守卫 stash 后即红）设计正确；ASAN 清零 + 原崩溃用例 rc=0 CONVERGED 端到端验证在案。

## 三、杂项核实

- `ctest -R read_rhog` 亲测 1/1 PASS；`MODULE_IO|constraint` 56 项中 3 失败（input_test_para/_para_4/read_item_serial），亲验失败原因为**陈旧 `sc_scf_thr_mode` 期望值**（测试期望 "threshold"，现默认 "immediate"，源自无关的 deltaspin 默认值统一）——确系预存在、与约束工作无关。**登记新待办：更新这三个目标的陈旧期望值（测试卫生，小修）**。

## 四、批复

1. **批准 commit 3**：`fix(io): guard out-of-basis plane waves in read_rhog (C-29)` + 回归测试 + spec/证据/dev-log；
2. **随后启动优先级 2**：inner_thr 三档扫描（1e-3/1e-4/1e-5 on 212/213）；
3. 顺手登记：MODULE_IO 三目标陈旧期望值修复（可并入任一后续 docs/test commit）。

---

## 本轮记录

- 评审轮，无代码改动。C-29 机制（越界写 −1 槽）、触发链（大基组电荷文件重启）、3 项 IO 失败归因（陈旧期望值）均经亲验。
