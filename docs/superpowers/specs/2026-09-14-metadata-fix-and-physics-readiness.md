# 元数据修复评审 + "能否开始有明确物理意义的测试"评估

> 评审对象：commit 31322f2fa + 两个待裁定项（parameters.yaml 重建、spin.md 刷新）+ 阶段状态评估。
> 本轮无代码改动。

## 一、31322f2fa 裁定：✅ 通过

四处漂移修复在案且范围纪律正确（只动元数据/注释，零运行时改动；--help 前后对比验证、56/56 不变；esolver_ks_lcao.cpp:583 同类陈述顺带修正、nsc 注释顺带修正——均属同族卫生，幅度可接受）。

## 二、两个待裁定项的批复

### 1. parameters.yaml 重建：**不授权，推迟到 merge-readiness 评审**
理由：docs/parameters.yaml 与 input-main.md 是**对外契约**。当前重建会首次公开 28 个 `deltap_*`（部分功能挂起/未验收）+ 12 个仍在演进中的 `constraint_*`（schema v1/v2 刚迁移完、偶极未实现、力的包络仍在扩展、阶段 B 未决）——在语义未冻结时发布参数文档，等于把开发态接口承诺为稳定接口。文件自 2026-05 未重建说明它本就跟随发布节奏而非提交节奏。**正确时点：feat/deltap 准备 PR 合入 develop 前的 merge-readiness 评审**（届时参数面冻结、一并重建）。/tmp 备查件保留即可。注：本次 metadata 修复已保证 --help 输出正确，生成文档滞后不阻断任何开发。

### 2. spin.md 的 DeltaSpin 参数表：**批准，独立 docs 小 commit**
≥6 处陈旧（nsc、已删除的 sc_scf_nmin、sc_scf_thr=1.0e-4、sc_drop_thr、sc_lambda_strategy 只剩 bfgs/linear_scan）——手写文档，无生成流程约束，按 `docs(deltaspin): refresh stale parameter table in spin.md` 独立提交。

## 三、核心问题：能否开始有明确物理意义的测试？

**裁定：可以——但只限于"不依赖力"的 SCF 级物理；力相关与 TM d 矩物理解读仍有两道闸门。**

### 已就绪（可以开始）

| 测试 | 物理意义 | 依据 |
|---|---|---|
| **III-1 电荷转移对 + Marcus 重组能**（H₂O 二聚体起步） | 框架的旗舰物理卖点（CDFT 接口：约束功 W、正反向 μ 差 → λ）——纯 SCF 级，只需约束态收敛 | A6 混合能力已验证；μ–Q 响应已被三个体系实测 |
| **I-1 扩展**（NaCl/其他离子体系的 κ 系统学） | "离子体系是电荷约束正当适用域"论断的体系化 | I-1 MgO 已闭合（线性区 ≥±0.8 e、κ 1.90–2.53） |
| Fe 基 DeltaSpin 交叉验证（SCF 级磁矩控制） | 与生产级既有实现的能力对等 | 12_PW_DS_S2_Z 现成基线 |

### 未就绪（必须先过闸门）

| 测试 | 闸门 | 状态 |
|---|---|---|
| 任何依赖力的物理（约束 relax/MD、几何驱动） | **V1 自旋力 FD 未闭合**：日志确认 18 腿在 2026-09-10 开跑后**无完成记录**（wave 1–2 后中断）；混合力 FD 未做 | 建议先用重设计处方（fixed-μ + release + LCAO 载体）补完 V1——成本已降到 ~1–2 h |
| TM d 局域矩的物理解读 | II-1b 脱钩（Becke 矩≠d 矩）未定量收口：4b 半径敏感性 + II-1 重锚定 | 在此之前，TM 自旋约束的结果只能按"Becke 加权矩"口径报告，不得写成"d 矩控制" |
| bulk 生产表征（V6 Au:Si） | Au 赝势资源缺口 | 待资源裁定 |

### 建议的起步组合（即可执行）

1. **III-1 H₂O 二聚体 CT + Marcus**（零资源门槛、物理意义最明确、顺带产出阶段 B 的耦合数据）；
2. **并行补完 V1**（自旋力 FD，重设计处方 ~1–2 h）——解锁后续力相关物理；
3. 4b/II-1b 随 II-1 重锚定合批推进。

---

## 本轮记录

- 评审轮，无代码改动。V1 未闭合系日志核查发现（(26) 后无完成条目，无归档结果）。
