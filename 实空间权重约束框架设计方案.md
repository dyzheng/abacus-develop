# 统一实空间权重约束框架：SMO 投影缺陷的诊断与 DeltaQ/DeltaSpin 改造方案

> 本文分四部分：第一部分总结 SMO 投影在共价体系中的失败诊断（含 toy model 数值验证）；第二部分给出改造路线的完整 taxonomy 与验收对比；第三部分详细展开**统一实空间权重约束框架**的算法方案（权重构造、势注入、μ 更新、力与应力、PW/LCAO 同口径实现）；第四部分为验证方案与风险控制。结论：**SMO 的病灶是"交叠密度无分账规则"，最小修复是全基 Löwdin 投影子，推荐终态是实空间 Hirshfeld/Becke 权重约束——它把电荷、自旋、偶极约束统一进同一框架。**

---

# 第一部分 问题诊断

## 1.1 SMO 投影的失效（事实回顾）

DeltaQ/DeltaSpin 的约束量为 SMO（单 zeta NAO 简单投影）电荷：

$$
N_I = \mathrm{Tr}[D\,\hat P_I],\qquad
\hat P_I = \sum_{l=0}^{L_{\max}}\sum_{m}|\alpha^I_{1,l,m}\rangle\langle\alpha^I_{1,l,m}|
$$

$\hat P_I$ 是逐轨道外积的简单求和，不含任何交叠矩阵 $S$ 的度量信息，因此 $\sum_I\hat P_I\neq\mathbb{1}$——不是单位分解。对归一占据轨道 $\psi$，其被总计数的次数为

$$
n(\psi)=\frac{c^\dagger S^2 c}{c^\dagger S c}\in[\lambda_{\min}(S),\ \lambda_{\max}(S)],
$$

其中 $c$ 是 $\psi$ 在非正交基下的系数向量。成键型态集中于 $S$ 的大本征值方向 → 系统性**过计**（如 H₂⁺ 成键轨道 $n=1+s$）；反键型态**欠计**（$n=1-s$）。净偏差的符号和大小取决于占据态谱。H₂O（全占据成键/孤对轨道）实测 $\sum_I N_I\approx 11.48$ vs 价电子 8，过计 44%；闭壳层下 $N_I\le 2\,\mathrm{Tr}\hat P_I$，O 的 SZV 子空间 4 个轨道给出人造天花板 8，实测约束封顶 7.91 正是顶在该界上——"投影子空间饱和"由此得到定量证明，目标越界后 μ 无界发散。

## 1.2 病灶的精确表述：交叠密度没有分账规则

把任意"原子电荷"方案写成统一形式：

$$
N_I = \underbrace{\sum_{i\in I}D^{S}_{ii}}_{\text{原子内项（无争议）}} + \underbrace{\sum_{i\in I}\sum_{j\notin I} w_{ij}\,D^{S}_{ij}S_{ij}}_{\text{交叠项（需分账）}},\qquad \text{sum rule 要求 } \sum_I w_{ij}=1
$$

**SMO 相当于对交叠项两边都取 $w=1$**：交叠密度被 I、J 全额各计一次。Mulliken 是 $w=1/2$；Löwdin 由 $S^{-1/2}$ 度量给出分账；Hirshfeld 在实空间逐点按 promolecule 密度比例分账。**任何满足 $\sum_I w_I=1$ 的分账规则都给出一个合法方案**——这就是"SMO 能否改造"的构造性答案，剩下的问题只是哪个规则同时满足约束计算的工程要求。

## 1.3 Toy model 数值验证（一维双原子共价键模型）

双原子（中心 ±1），最小集轨道宽度 1，另加一个宽度 2.2 的弥散第二 zeta；交叠 $\langle s_A|s_B\rangle=0.368$（典型共价量级），闭壳层 2 电子非对称极性键。结果：

| 方案 | $N_A$ | $N_B$ | $\Sigma$ | 判定 |
|---|---|---|---|---|
| SMO 简单投影（仅第一 zeta） | 1.595 | 1.076 | **2.671**（应为 2） | 过计 34% |
| 全基组全局 Löwdin | 1.505 | 0.495 | **2.000** | 精确 |
| 仅 SZV 子空间 Löwdin | — | — | **1.974** | 欠计（第二 zeta 分量漏出子空间） |
| 斜投影 / Mulliken | 1.463 | 0.537 | **2.000** | 精确但算子非正定 |
| Mulliken 病态示例（反键混入） | 2.092（>2） | −0.092（<0） | 2.000 | 布居越界 |
| Hirshfeld 实空间权重 | 1.240 | 0.760 | **2.000**（逐点精确） | 非负、无饱和 |

另验证：Hirshfeld 权重 $w_A$ 在 B 核处仅 0.018——权重局域性良好。

## 1.4 更深一层：模型层不适配（共价极化问题）

分子偶极分解 $\boldsymbol\mu=\sum_I q_I\mathbf R_I+\sum_I\boldsymbol\mu_I^{\rm at}$：DeltaQ 的单极子变量只能控制第一项（电荷转移项），而共价分子极化的主通道是第二项（键偶极/孤对形变）。单极子约束参数化不了偶极响应——这是选择状态变量层面的问题，与测量口径无关。推论：共价 POLAR 问题应改用有限电场（偶极约束的 Lagrange 对偶）与多极子分析语言；DeltaQ 的正当适用域是电荷局域、位点可分辨的离子/缺陷体系与片段间电荷转移。

---

# 第二部分 改造路线 taxonomy 与验收对比

## 2.1 约束变量的工程验收标准

| # | 标准 | 理由 |
|---|---|---|
| C1 | Sum rule：$\sum_I N_I=N_{\rm val}$ 精确 | 约束目标才有化学对应物 |
| C2 | 非负、无虚假饱和 | 可达范围宽、μ 不发散 |
| C3 | 约束算符局域 | μ 是局域势，物理可控 |
| C4 | 基组/表示无关（PW≡LCAO） | 消除两套口径 |
| C5 | 对 $D$ 线性 | SCF 结构不变、力干净 |
| C6 | 对位置可微 | 几何优化/MD 可用 |

## 2.2 候选路线

**路线 A：全基组全局 Löwdin（轨道空间最小改动）。** 投影子换成 $|\tilde\alpha\rangle=S^{-1/2}|\alpha\rangle$（$S$ 为跨原子全基组交叠矩阵），$N_I=\sum_{i\in I}(S^{1/2}DS^{1/2})_{ii}$。Sum rule 精确；Löwdin 轨道是偏离原 NAO 最小的正交集，局域性保留好；改造量小（换 `pre_hr` 组装）。风险：弥散基近线性相关 → $S^{-1/2}$ 需本征值截断正则化；仍基组依赖，PW/LCAO 口径仍分裂。更讲究的同类：NAO（NBO 的自然原子轨道）/ QUAMBO，化学解释更干净但构造复杂。

**路线 B：仅 SZV 子空间正交化——半修，不建议。** 只恢复 SZV 子空间内部的单位分解，占据轨道漏在子空间外的分量导致**由过计矫枉为欠计**（toy model 欠计 1.3%）。这也解释了 PW `OnsiteProjector`（口径更窄）与 LCAO 结果反向偏离的现象。

**路线 C：双正交/斜投影（$|\alpha\rangle\langle\beta|$，$\langle\beta_i|\alpha_j\rangle=\delta_{ij}$）——排除作约束。** 数学上等价 Mulliken：sum rule 精确，但算子非 Hermitian（对称化后非正定），布居可 >2 或 <0（toy model 已演示）。作分析工具可，作约束算符病态（$\mu\hat P$ 非良行为吸引/排斥势）。

**路线 D：实空间权重（Hirshfeld / Becke）——推荐终态。** 见第三部分。

## 2.3 验收总表

| 方案 | C1 sum rule | C2 非负/无饱和 | C3 局域 | C4 表示无关 | C5 线性 | C6 可微 | 结论 |
|---|---|---|---|---|---|---|---|
| SMO 现状 | ✗（过计） | ✗（假饱和） | ✔ | ✗ | ✔ | ✔ | 病灶 |
| SZV 子空间正交化 | △（欠计） | ✔ | ✔ | ✗ | ✔ | ✔ | 半修 |
| 斜投影/Mulliken | ✔ | ✗（可负可>2） | △ | ✗ | ✔ | ✔ | 仅分析 |
| 全基 Löwdin | ✔ | △ | ✔ | ✗ | ✔ | ✔ | 最小改动解 |
| **Hirshfeld/Becke 实空间** | ✔✔（逐点） | ✔ | ✔ | ✔ | ✔ | ✔ | **推荐终态** |

---

# 第三部分 统一实空间权重约束框架：算法方案

## 3.1 框架定义

把所有约束写成统一形式——**空间权重 $w_\alpha(\mathbf r)$ × 密度通道 $d_\alpha(\mathbf r)$**：

$$
Q_\alpha = \int w_\alpha(\mathbf r)\, d_\alpha(\mathbf r)\, d\mathbf r
$$

| 约束类型 | 权重 $w_\alpha$ | 通道 $d_\alpha$ | 对应现有功能 |
|---|---|---|---|
| 原子电荷 | 原子权重 $w_I(\mathbf r)$，$\sum_I w_I\equiv1$ 逐点 | 电荷密度 $\rho$ | DeltaQ |
| 片段电荷 | $w_F=\sum_{I\in F}w_I$ | $\rho$ | DeltaQ（多原子组） |
| 原子自旋矩 | $w_I(\mathbf r)$ | 磁化密度 $m=\rho_\uparrow-\rho_\downarrow$ | DeltaSpin |
| 片段自旋 | $w_F$ | $m$ | DeltaSpin |
| 偶极分量 | $w=(z-z_0)$（位置算子） | $\rho$ | 新功能（对偶于有限电场） |
| 四极/高阶矩 | $(z-z_0)^2$ 等 | $\rho$ | 新功能 |

片段划分若覆盖全部原子，片段间仍满足 sum rule；原子与片段约束可任意混合，全部走同一条代码路径。偶极约束与外电场互为 Lagrange 对偶：约束乘子即场强，这为共价 POLAR 类问题提供了绕过"分电荷模型"的正道。

## 3.2 权重函数构造

### 3.2.1 Becke 模糊 Voronoi 分区（首选：无参数密度、纯几何、$C^\infty$ 光滑）

对每个网格点 $\mathbf r$：

$$
\mu_{ij}=\frac{r_i-r_j}{\chi_{ij}R_{ij}},\quad r_i=|\mathbf r-\mathbf R_i|,\ R_{ij}=|\mathbf R_i-\mathbf R_j|
$$

$$
s(\mu)=\tfrac12\bigl[1-f_3(\mu)\bigr],\qquad f_1(\mu)=\tfrac12(3\mu-\mu^3),\quad f_k=f_1\circ f_{k-1}
$$

$$
P_i(\mathbf r)=\prod_{j\neq i}s(\mu_{ij}),\qquad w_i(\mathbf r)=\frac{P_i(\mathbf r)}{\sum_k P_k(\mathbf r)}
$$

$\chi_{ij}$ 为异核半径比修正（可用共价半径表）。$f_3$ 使权重在原子间三倍可微、切换陡峭；分母归一化保证**逐点** $\sum_i w_i=1$。所有导数解析可得。不需要任何原子密度输入。

### 3.2.2 Hirshfeld 前分子分区（次选：化学适应性更好）

$$
w_I(\mathbf r)=\frac{\rho_I^0(|\mathbf r-\mathbf R_I|)}{\displaystyle\sum_J\rho_J^0(|\mathbf r-\mathbf R_J|)}
$$

$\rho_I^0$ 为球平均自由原子密度（按元素/赝势在径向网格上列表，一次生成反复复用——ABACUS 初始电荷猜测已有一套原子密度，可直接复用其数据源）。分母加阈值保护（$\sum\rho^0<10^{-12}$ 的远区直接均分或按最近原子归属）。**用固定 promolecule（对 $D$ 线性）作约束形态；Hirshfeld-I/ISA 迭代版本只用于后分析**（权重依赖密度 → 约束非线性，SCF Jacobian 与力都要加响应项，得不偿失）。

### 3.2.3 偶极权重与周期边界

$w=z-z_0$（$z_0$ 取盒中心或电荷中心）。周期体系下位置算子无定义：分子/团簇用大盒直接可用；体相需换 sawtooth 势或 Berry 相位形式，本框架内以"非周期偶极约束"为第一期范围，明确标注。

## 3.3 能量、势注入与符号约定

Lagrange 形式：

$$
\mathcal L = E_{\rm KS}[\rho] + \sum_\alpha \mu_\alpha\bigl(Q_\alpha - t_\alpha\bigr)
$$

对 $\rho$ 变分得 Kohn–Sham 势修正（**关键：只是一个网格上的标量势**）：

$$
\boxed{\;V_{\rm eff}(\mathbf r)\;\leftarrow\; V_{\rm eff}(\mathbf r) + \sum_\alpha \mu_\alpha\, w_\alpha(\mathbf r)\;}
$$

符号约定：$\mu_\alpha>0$ 抬升 $w_\alpha$ 区势能 → 电子流出 → $Q_\alpha$ 减小。自旋约束（通道 $m$）按符号注入两个自旋通道：$V_\uparrow \mathrel{+}= \mu\, w_I$，$V_\downarrow \mathrel{-}= \mu\, w_I$。报告总能中

$$
E_{\rm con}=\sum_\alpha \mu_\alpha\bigl(Q_\alpha-t_\alpha\bigr)
$$

（沿用 `escon` 的形式，仅替换 $N_I$ 的口径）。**由于 $w_\alpha\in[0,1]$（偶极权重除外），$Q_\alpha$ 非负、无 SZV 式人造天花板，μ 发散的几何根源被移除。**

罚函数可选模式（防残余不收敛、平滑 SCF）：

$$
E_{\rm pen}=\sum_\alpha \frac{k_\alpha}{2}\bigl(Q_\alpha-t_\alpha\bigr)^2
\;\Rightarrow\; \mu_\alpha^{\rm eff}=k_\alpha\bigl(Q_\alpha-t_\alpha\bigr)
$$

有效乘子自动随残差线性收缩，SCF 鲁棒；代价是约束近似满足（残差 $\sim\mu/k$）。硬约束与罚函数共享同一代码路径，仅 μ 更新规则不同。

## 3.4 SCF 流程与 μ 更新

两环结构（与现有 DeltaQ 两阶段 delta 模式兼容）：

1. **预处理（每个几何一次）**：在实空间网格构造全部 $w_\alpha$ 并缓存；逐点检查 $\sum_I w_I=1$ 至机器精度。
2. **参考态**：无约束 SCF，记录自然值 $Q_\alpha^{\rm ref}$ 并打印 sum rule 自检 $\sum_I N_I$ vs $N_{\rm el}$（沿用原文档 §6 判据，此时应恒成立）。absolute / delta 两种目标模式：delta 模式 $t_\alpha=Q_\alpha^{\rm ref}+\Delta_\alpha$。
3. **约束 SCF**：每电子步把 $V_{\rm con}(\mathbf r)=\sum_\alpha\mu_\alpha w_\alpha(\mathbf r)$ 并入局域势（见 3.6 两条实现路径）。
4. **约束外环**：SCF 收敛（或到内环步数上限）后，读 $Q_\alpha$，更新 $\boldsymbol\mu$：

   - 单约束：secant，$\mu^{n+1}=\mu^n+\lambda\,(Q^n-t)$，$|\Delta\mu|$ 截断（如 0.05 Ry）；
   - 多约束：向量 Broyden，Jacobian $J_{\alpha\beta}=\partial Q_\alpha/\partial\mu_\beta$ 由历史对 $\{(\boldsymbol\mu^k,\boldsymbol Q^k)\}$ 在线估计（近刚性：$J$ 主对角占优，不同原子的权重几乎不正交干扰——$w_I$ 局域）；
   - 收敛判据：$\max_\alpha|Q_\alpha-t_\alpha|<10^{-4}\,e$ 且 SCF 收敛；
   - **防发散熔断**：若 $|\mu_\alpha|>\mu_{\max}$（如 5 Ry）仍未达标，判定目标不可达并报告 $Q_\alpha(\mu)$ 扫描曲线端点，而非放任发散（直接修复原文档 μ→−1244 Ry 的工程病）。
5. **输出**：$Q_\alpha$、$\mu_\alpha$、$E_{\rm con}$、sum rule 审计行。

## 3.5 力与应力

SCF 收敛后由 Hellmann–Feynman，约束对原子 $J$ 的力只有显式项：

$$
\mathbf F_J^{\rm con}=-\sum_\alpha \mu_\alpha\int \rho(\mathbf r)\,\frac{\partial w_\alpha(\mathbf r)}{\partial \mathbf R_J}\, d\mathbf r
$$

- Hirshfeld：$\dfrac{\partial w_I}{\partial \mathbf R_J}=\dfrac{\delta_{IJ}\,\nabla_J\rho_I^0\, P - \rho_I^0\,\nabla_J\rho_J^0}{P^2}$，其中 $\nabla_J\rho_J^0=-\nabla_{\mathbf r}\rho_J^0$，由径向表 $d\rho^0/dr$ 与方向余弦在网格上解析组装；
- Becke：$s(\mu)$、$P_i$ 的导数全部解析；
- 偶极权重：$\partial w/\partial R_J=0$，约束力为零（这正是外电场的已知性质；$z_0$ 若取电荷中心则需链式项，建议固定 $z_0$）。

应力：约束项无显式体积依赖（权重随原子刚性移动），对应力无贡献；固定网格下导数处理与局域赝势相同。

## 3.6 PW / LCAO 同口径实现路径（ABACUS 落点）

| 环节 | PW 路径 | LCAO 路径 |
|---|---|---|
| 势注入 | $V_{\rm con}$ 直接加进 veff 实空间网格（与局域赝势同路径） | 经既有网格数值积分（XC 同一路径）组装矩阵元 $H^{\rm con}_{\mu\nu}=\int \alpha_\mu V_{\rm con}\alpha_\nu\,d\mathbf r$ |
| 约束读数 | $Q_\alpha=\sum_{\mathbf r}w_\alpha\rho$（密度网格） | 同左——**读数也在网格上做，不进基组** |
| 关键收益 | — | 读数与基组解耦 → PW 与 LCAO **逐位一致**，原文档的 `OnsiteProjector` vs `pre_hr` 口径分裂问题自动消失 |

被替换/新增模块映射：`cal_moment`/`pre_hr`（SMO 投影）→ `weight_grid`（新模块，权重构造+缓存）；`escon` 形式不变；μ 更新环沿用 `spin_constrain` 外环骨架；ΔSpin 复用同权重、换通道。输入语义变更需文档化：`tc` 从"SZV 投影电荷"变为"Hirshfeld/Becke 电荷"，绝对目标值量级变小（H₂O 的 Hirshfeld 电荷 O 约 −0.2~−0.3 e），一律推荐 delta 模式。

## 3.7 数值细节

- **网格**：权重光滑（Becke $C^3$、Hirshfeld 随原子密度衰减），复用现有密度网格即可，无需加细；
- **并行**：$w_\alpha$ 按网格域分解局域计算，零通信；构造耗时 $O(N_{\rm grid}\times N_{\rm atom})$（Becke 为原子对），相对 SCF 可忽略；
- **周期性**：promolecule 密度按周期像求和（衰减快，近邻像即收敛）；
- **缓存**：几何不变时 $w$ 及其对 $R_J$ 的导数模板均可缓存；MD 中每步重构耗时亦可忽略；
- **内存**：每约束一份网格数组（双网格可只存粗网格）。

## 3.8 伪代码

```
# 预处理（每几何一次）
for I in atoms: 构造 w_I(r)          # Becke 或 Hirshfeld(promolecule)
assert max_r |Σ_I w_I(r) - 1| < 1e-12

# 参考态
run_scf(V_con = 0)  →  Q_ref, 打印 Σ_I N_I vs N_el 审计行
t = target(Q_ref, mode=absolute|delta)

# 约束外环
μ = 0; 历史 = []
loop:
    run_scf(V_con = Σ_α μ_α w_α)     # 势注入（PW: veff 网格; LCAO: 网格积分矩阵元）
    Q = ∫ w_α d_α(r) dr
    if max|Q - t| < tol and scf_converged: break
    if max|μ| > μ_max: 报告不可达 + Q(μ) 端点; break   # 熔断
    μ = broyden_update(μ, Q, t, 历史) # 或罚函数模式 μ = k(Q-t)

# 收尾
E_con = Σ_α μ_α (Q_α - t_α)
F_J  += -Σ_α μ_α ∫ ρ ∂w_α/∂R_J dr
```

---

# 第四部分 验证方案与风险

## 4.1 验证矩阵

| # | 测试 | 方法 | 通过标准 |
|---|---|---|---|
| V1 | Sum rule | H₂O 无约束 SCF，打印 $\sum_I N_I$ | 恒等于 8（逐点权重归一已断言） |
| V2 | 自然电荷基准 | H₂O Hirshfeld 电荷 vs 文献/既有代码 | O ∈ [−0.35, −0.20] e，PW≡LCAO 逐位一致 |
| V3 | 可达性/线性 | delta 模式小 q 扫描（±0.05~0.3 e） | ΔQ 全达标、无封顶；偶极响应 vs q 单调 |
| V4 | 偶极对偶 | 偶极约束（$t=\mu_z$）vs 显式有限电场 | 两法所得 $\rho(\mathbf r)$、$E$ 曲线一致（Lagrange 对偶交叉验证） |
| V5 | 力 | 解析力 vs 有限差分 | 误差 < 1e-4 Ha/Bohr |
| V6 | 回归 | Au:Si 电荷态扫描（原 DeltaQ 本职场景） | 与原 SMO 结果趋势一致、口径偏移可解释 |
| V7 | 熔断 | 故意设不可达目标 | 报告不可达 + 端点，μ 有界 |

## 4.2 风险与开放问题

1. **参考密度依赖**（Hirshfeld）：自由原子 vs 带电参考改变口径——用 delta 模式吸收；Becke 无此问题但半径表引入另一组参数。
2. **Hirshfeld 电荷系统性偏小**：绝对目标需重新定标，用户文档必须强提示。
3. **偶极约束的 PBC 推广**：一期仅限分子/大盒；体相 sawtooth/Berry 相位留二期。
4. **非共线自旋**：权重框架可推广（矢量 $m$ 通道），但需额外设计，暂不承诺。
5. **强关联/共价极化的模型层问题仍在**：本框架修的是测量层（sum rule + 口径统一）；共价 POLAR 物理本身请走偶极约束/有限电场通道（3.2.3、V4），不要退回单极子电荷语言。

## 附录 A 符号约定一览

- $D$：密度矩阵；$D^S$：其在非正交基下的表示；$S$：全基组交叠矩阵
- $w_\alpha(\mathbf r)$：约束 $\alpha$ 的空间权重；$d_\alpha$：密度通道（$\rho$ 或 $m$）
- $\mu_\alpha$：Lagrange 乘子（约束势强度）；$t_\alpha$：目标值；$Q_\alpha$：约束观测量
- 势注入约定：$V_{\rm eff}\leftarrow V_{\rm eff}+\sum_\alpha\mu_\alpha w_\alpha$；$\mu>0$ 排斥该权重区电子

## 附录 B 从 SMO 到本框架的迁移清单

1. 新模块 `weight_grid`：Becke/Hirshfeld 权重构造、逐点归一断言、位置导数、缓存
2. `spin_constrain` 外环：μ 更新换 secant/Broyden + 熔断；罚函数开关
3. 势注入：PW 走 veff 网格；LCAO 走网格积分矩阵元（XC 同路径）
4. 读数：全部改为网格积分 $Q_\alpha=\int w_\alpha d_\alpha$；删除/保留 `pre_hr` 仅作旧口径对照输出
5. 输入：`tc` 语义文档化，新增 `weight_type = becke|hirshfeld`、`constraint_type = charge|spin|dipole`、`fragment` 分组、`mu_max`
6. 每 SCF 打印 sum rule 审计行（$\sum_I N_I$ vs $N_{\rm el}$）作为永久自检

---

# 第五部分 原子受力修正：完整推导

## 5.1 包络定理推导：为什么只剩显式项

把约束问题的驻点写成对密度与乘子的鞍点（固定几何 $\{\mathbf R\}$）：

$$
\mathcal L[\rho,\boldsymbol\mu;\{\mathbf R\}] = E_{\rm KS}[\rho;\{\mathbf R\}] + \sum_\alpha \mu_\alpha\bigl(Q_\alpha[\rho;\{\mathbf R\}]-t_\alpha\bigr)
$$

约束极小能量 $E^*(\mathbf t,\{\mathbf R\})=\mathcal L|_{\rho^*,\boldsymbol\mu^*}$，其中 $\rho^*$ 由 KS 方程（势中含 $\sum_\alpha\mu_\alpha w_\alpha$）给出，$\boldsymbol\mu^*$ 由 $Q_\alpha=t_\alpha$ 定出。对 $\mathbf R_J$ 求全导：

$$
\frac{dE^*}{d\mathbf R_J} = \underbrace{\frac{\partial\mathcal L}{\partial\mathbf R_J}}_{\text{显式}} + \underbrace{\int\frac{\delta\mathcal L}{\delta\rho}\frac{\partial\rho^*}{\partial\mathbf R_J}d\mathbf r}_{=0\ (\text{SCF 驻点})} + \underbrace{\sum_\alpha\frac{\partial\mathcal L}{\partial\mu_\alpha}\frac{d\mu_\alpha^*}{d\mathbf R_J}}_{=\,(Q_\alpha-t_\alpha)\,d\mu_\alpha^*/d\mathbf R_J\;=\;0\ (\text{约束精确满足})}
$$

两个隐式项分别被 **SCF 驻点条件**和**约束精确满足**消去（包络定理）。于是约束力只有显式项：

$$
\boxed{\;\mathbf F_J^{\rm con}=-\frac{dE^*_{\rm con}}{d\mathbf R_J}=-\sum_\alpha \mu_\alpha\int d_\alpha(\mathbf r)\,\frac{\partial w_\alpha(\mathbf r)}{\partial \mathbf R_J}\,d\mathbf r\;}
$$

两个重要推论：

1. **$d\boldsymbol\mu^*/d\mathbf R_J$ 不出现**——不需要对乘子做响应求解，约束力计算廉价且严格。前提是外环已收敛（$|Q-t|$ 在容差内）；否则该误差项正比于残差 $|Q-t|$，由收敛容差控制。
2. **罚函数/部分收敛模式**：用 $\mu_\alpha^{\rm eff}=k_\alpha(Q_\alpha-t_\alpha)$ 代入同一公式即可，残余误差 $O(|Q-t|)$。

## 5.2 Hirshfeld 权重的闭式力：一个紧凑形式

记 $p_I(\mathbf r)=\rho_I^0(s_I)$，$s_I=|\mathbf r-\mathbf R_I|$，$P=\sum_K p_K$，$w_I=p_I/P$，$\hat{\mathbf e}_I=(\mathbf r-\mathbf R_I)/s_I$，$p_I'=dp_I/ds_I$。由 $\partial p_K/\partial\mathbf R_K=-p_K'\hat{\mathbf e}_K$：

$$
\frac{\partial w_I}{\partial \mathbf R_J}
= \frac{\delta_{IJ}\,(-p_I'\hat{\mathbf e}_I)\,P + p_I\,p_J'\hat{\mathbf e}_J}{P^2}
= -\delta_{IJ}\frac{p_I'\hat{\mathbf e}_I}{P} + w_I\,\frac{p_J'\hat{\mathbf e}_J}{P}
$$

代入 5.1 的结果，并利用 $\sum_I\mu_I w_I=V_{\rm con}(\mathbf r)$，得**闭式**：

$$
\boxed{\;\mathbf F_J^{\rm con}=\int \rho(\mathbf r)\,\underbrace{\frac{p_J'(s_J)}{P(\mathbf r)}\,\hat{\mathbf e}_J}_{\displaystyle\equiv\,\mathbf g_J(\mathbf r)}\,\bigl[\mu_J - V_{\rm con}(\mathbf r)\bigr]\,d\mathbf r\;}
$$

物理图像清晰：

- $\mathbf g_J(\mathbf r)$ 是局域在原子 J"密度梯度皮肤"（自由原子密度变化区）内的矢量场——**约束力只作用在原子 J 的壳层区域**，远离 J 处自动为零；
- 驱动项是 **$[\mu_J-V_{\rm con}(\mathbf r)]$**：本原子乘子与该点总约束势之差。若某点附近有其他原子的约束势占优，它通过 $V_{\rm con}$ 进入 J 的力——多约束耦合在力层面自动正确处理；
- 单约束特例：若只对 J 加约束且 $w_J$ 在 J 皮肤内近似为 1，则 $\mu_J-V_{\rm con}\approx\mu_J(1-w_J)\approx0$，力趋于零——**对自洽区深处的纯整体充电，约束力小**，符合"电荷加到整个片段不搬动原子"的直觉。

全部量（径向表 $p_J'$、方向余弦、网格上的 $P$、$\rho$）在网格上局域组装，与 XC 网格积分同路径，零额外通信。

## 5.3 Becke 权重的力

$w_i=P_i/\sum_kP_k$、$P_i=\prod_{j\neq i}s(\mu_{ij})$ 的导数由链式法则解析给出：$\partial s/\partial\mu$ 是 $f_3$ 的导数（多项式，闭式），$\partial\mu_{ij}/\partial\mathbf R_J$ 是几何量（距离与距离差的导数，闭式）。无需求数值梯度。验证走 V5（解析力 vs 有限差分，< 1e-4 Ha/Bohr）。

## 5.4 无 Pulay 附加项与应力

- 权重 $w_\alpha$ 定义在**实空间、不依赖基组** → LCAO 下约束项**不引入新的 Pulay 项**（普通 KS 部分原有的基组 Pulay 项照旧，与本框架无关）；PW 下亦然。这是相对轨道投影方案的又一个结构性优势：投影子的位置导数（NAO 随原子移动且非正交基求导）整条链路被绕过。
- **应力**：权重随原子刚性平移、对晶胞无形变依赖 → 约束项对应力无显式贡献（固定网格的处理与局域赝势一致）。偶极权重 $w=z-z_0$ 在应变下形变，若未来需要约束应力的体相推广，再引入应变耦合项；分子体系一期不涉及。

---

# 第六部分 拉格朗日乘子 $\mu$ 的物理意义

## 6.1 基本定理：μ 是"约束方向的热力学力"

由包络定理，对驻点值 $E^*(\mathbf t)$ 求目标参数的导数：

$$
\boxed{\;\mu_\alpha=-\frac{\partial E^*}{\partial t_\alpha}\;}
$$

即：**$\mu_\alpha$ 是把约束目标再推远一个单位所需能量的负梯度**——维持这份电荷/自旋转移，体系每单位转移要付出的能量代价。它不是数值 artifact，而是与温度、压力同级的热力学共轭变量。

## 6.2 电学解释：μ 就是外加的局部电压

在本框架约定 $V_{\rm eff}\leftarrow V_{\rm eff}+\sum_\alpha\mu_\alpha w_\alpha$ 下，$\mu_I$ 是**施加在 $w_I$ 区域的局部电势移动**。物理等价物：

- 电子化学势在无约束平衡时处处相等；约束的本质是**人为维持两个区域间的化学势差**——$\mu$ 差就是这块"外接电池"的电压；
- 量纲即电压：$\mu$ 以 Ry 计时，$1\ {\rm Ry}/e = 13.6\ {\rm V}$。于是原文档的 $\mu_O\to-1244$ Ry 可读作"试图对氧原子加 −17 kV"——**μ 的量级本身就是不可达性诊断**：正常化学充电（零点几电子）对应 $|\mu|$ 在 0.01–0.1 Ry（0.1–1 V）量级，超出即应触发熔断（3.4 节）；
- 符号：$\mu_I>0$ 抬升该区能级 → 电子流出 → $Q_I$ 减小。

## 6.3 响应解释：μ–Q 扫描顺带测出硬度矩阵（概念 DFT 接口）

外环 Broyden 在线估计的 Jacobian 不是数值副产品，而是物理量：

$$
J_{\alpha\beta}=\frac{\partial Q_\alpha}{\partial\mu_\beta}
$$

是体系对**局域势**的静态电荷响应矩阵（condensed charge susceptibility）；其逆阵即**硬度矩阵**（Pearson 硬度 $\eta$ 的位点分辨推广），迹给出分子软度信息。换言之：**一次 $\mu$–$Q$ 扫描 = 一次线性响应测量**，ΔQ 计算顺带产出概念 DFT 的全部位点分辨描述符（局域硬度、软度、亲电/亲核指数的方向性）。这是本框架相对"只输出一个约束能"的 SMO 实现的增量价值。

## 6.4 与电荷转移理论（CDFT/Marcus）的接口

对给体–受体电荷转移透热态，标准 CDFT 结果表明：透热态的垂直能隙 ≈ $\sum_\alpha\mu_\alpha\Delta Q_\alpha$；正反向两次约束计算给出的 $\mu$ 差可直接估计 Marcus 重组能 $\lambda$。约束功 $W=\sum_\alpha\mu_\alpha(Q_\alpha-Q_\alpha^{\rm ref})$ 应作为标准输出（见第七部分），它是连接约束计算与电荷转移动力学的桥梁量。

## 6.5 工程判据小结

| μ 的表现 | 解读 | 动作 |
|---|---|---|
| $|\mu|\lesssim0.1$ Ry 且随 $\|Q-t\|$ 光滑变化 | 正常约束区 | 继续 |
| μ–Q 曲线斜率突变 | 接近可达边界/电子结构重排（如轨道重排、自旋翻转） | 报告事件，检查占据 |
| $|\mu|>\mu_{\max}$（如 5 Ry ≈ 68 V） | 目标不可达 | 熔断，输出 $Q(\mu)$ 端点 |
| 约束满足但 μ 对 $t$ 无响应 | 约束落在密度零空间（如真空区权重） | 检查权重构造 |

---

# 第七部分 约束成功后的有效标签（输出规范）

能量、力、应力之外，一次成功的约束计算应输出四组标签。设计原则：**每个标签都要么是可自检的质量证书，要么是可直接进入下游物理分析的量**。

## 7.1 约束质量标签（"这次约束可信吗"）

| 标签 | 定义 | 用途 |
|---|---|---|
| 残差 $\|Q_\alpha-t_\alpha\|$ | 逐约束 | 约束精度证书（硬约束 < 1e-4 e） |
| sum rule 审计行 | $\sum_I N_I$ vs $N_{\rm el}$（逐 SCF 打印） | 口径自检，永久开启 |
| 可达性余量 | $(Q_\alpha^{\max}-Q_\alpha)/(Q_\alpha^{\max}-Q_\alpha^{\rm ref})$，$Q^{\max}$ 由熔断扫描端点估计 | 距离饱和边界多远，防止重蹈 SZV 封顶 |
| 线性度证书 | $\mu$–$Q$ 扫描局部斜率的相对变化 | 判定是否处在线性响应区（指纹检验的前提） |
| 收敛元数据 | 外环迭代数、是否触熔断、罚函数残差 | 排查与复现 |

## 7.2 物理标签（"这次约束对应什么物理"）

| 标签 | 定义 | 用途 |
|---|---|---|
| $\mu_\alpha$（eV/V 双单位） | 约束乘子 | 局部电压读数；不可达诊断 |
| 约束功 $W$ | $\sum_\alpha\mu_\alpha\Delta Q_\alpha$ | Marcus 分析、透热态能隙估计 |
| 电荷响应矩阵 $\partial Q_\alpha/\partial\mu_\beta$ | Broyden Jacobian 输出 | 位点分辨硬度/软度（概念 DFT） |
| 诱导多极子 | 约束态 vs 参考态的偶极/四极变化 | 长程静电/C₃ 指纹的正确观测量 |
| 分子级描述符 | $\eta=\tfrac12(\partial\mu/\partial Q)^{-1}$ 型汇总 | 化学反应性指数 |

## 7.3 电子结构标签（"电荷从哪来、到哪去"）

| 标签 | 定义 | 用途 |
|---|---|---|
| 权重投影 DOS | $\rho_I(\varepsilon)=\sum_n\langle\psi_n|w_I|\psi_n\rangle\delta(\varepsilon-\varepsilon_n)$，$\sum_I\rho_I=\rho_{\rm DOS}$ 严格 | "哪个原子的哪些态"承接了转移电荷 |
| 逐带电荷分解 | $\Delta Q_{n,I}=\langle\psi_n|w_I|\psi_n\rangle$ 约束态 vs 参考态 | 识别前沿轨道（HOMO/LUMO）主导的转移通道 |
| 密度差图 $\Delta\rho(\mathbf r)$ | 约束态 − 参考态，cube 输出 | 直观检查转移的空间形态是否合理 |
| 局域自旋矩 $M_I$ | $\int w_I m$（ΔSpin 通道） | 磁性体系的矩标签 |
| 轨道重排事件标记 | 占据数/能级穿越检测 | μ–Q 斜率突变的物理解释 |

## 7.4 可复现性标签（"口径是什么"）

权重类型（`becke|hirshfeld`）、半径表或 promolecule 密度来源与版本哈希、目标模式（absolute/delta）、容差与 $\mu_{\max}$、代码版本——**因为本框架的电荷口径由权重定义决定，任何数字脱离权重元数据都不可引用**。建议把以上四组标签以机器可读块（JSON/YAML）写入标准输出，作为后续数据集构建（如 C₃ 指纹拟合）的接口。

---

> 至此方案闭环：第一、二部分回答"为什么改、往哪改"；第三部分给出框架算法；第五部分给出力的严格推导与闭式；第六部分把 μ 从数值参数升格为物理观测量；第七部分定义输出契约，使约束计算从"能跑"升级为"可审计、可解释、可复现"。
